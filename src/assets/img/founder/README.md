# Foto del fundador

Suelta aqui la foto en formato `.jpg`, `.jpeg`, `.png` o `.webp`.

Nombre recomendado: `founder.jpg` (cuadrada ~600x600px).

Se carga automaticamente en el componente `FounderSection` mediante `import.meta.glob`.

Si no hay foto, el componente muestra un avatar placeholder con iniciales (D.).
